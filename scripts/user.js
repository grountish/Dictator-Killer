'use strict'

class User {
  constructor(name, email, password, score, currentUser) {
    this.name = name;
    this.email = email;
    this.password = password;
    this.score = score;
    this.currentUser = currentUser;

  }
}